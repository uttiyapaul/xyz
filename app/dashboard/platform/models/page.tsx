import { PlatformModelsView } from "@/features/platform/models/PlatformModelsView";

export const dynamic = "force-dynamic";

/**
 * Thin route entry for the platform models and digital twin workspace.
 */
export default function PlatformModelsPage() {
  return <PlatformModelsView />;
}
