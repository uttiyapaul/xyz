import { SkeletonPage } from "@/components/layout/SkeletonPage";

export default function DataAiExtractPage() {
  return (
    <SkeletonPage 
      title="AI Invoice Parsing" 
      description="Upload raw PDFs and utility bills. All files are subjected to deep malware and vulnerability scans before data is automatically extracted to the ledger."
    />
  );
}
